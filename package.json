{
  "name": "festos",
  "private": true,
  "description": "Workspace root: commit hooks, commit message rules and formatting shared by the front end and end-to-end tests.",
  "packageManager": "pnpm@12.6.0",
  "engines": {
    "node": ">=24.18.1"
  },
  "scripts": {
    "prepare": "lefthook install",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@commitlint/cli": "21.2.3",
    "@commitlint/config-conventional": "21.2.3",
    "lefthook": "2.1.14",
    "prettier": "3.9.8"
  }
}
