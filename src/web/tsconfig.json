{
  "files": [],
  "references": [{ "path": "./tsconfig.app.json" }, { "path": "./tsconfig.node.json" }],
  // Read by the shadcn CLI to resolve the "@/" alias; the build uses tsconfig.app.json.
  "compilerOptions": {
    "paths": { "@/*": ["./src/*"] }
  }
}
